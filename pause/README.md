# Pause Pods create tool

## Create pause pods

Format:

```bash
./pause.sh apply <initial ID> <final ID>
```

Example:

```bash
./pause.sh apply 1 100
```

## Delete pause pods

Format:

```bash
./pause.sh delete <initial ID> <final ID>
```

Example:

```bash
./pause.sh delete 1 100
```
